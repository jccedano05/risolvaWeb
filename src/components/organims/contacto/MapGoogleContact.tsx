import { Grid, Typography } from "@mui/material";
import React from "react";
import { GoogleMap, withScriptjs, withGoogleMap } from "react-google-maps";

export const MapGoogleContact = () => {
  return (
    <Grid container>
      <Typography variant="h3">Mapa</Typography>
    </Grid>
  );
};
