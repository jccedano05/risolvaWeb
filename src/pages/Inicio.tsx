import React from "react";
import { Typography } from "@mui/material";
import { InProcess } from "./InProcess";

export const Inicio = () => {
  return (
    <div>
    <InProcess />
    </div>
  );
};
