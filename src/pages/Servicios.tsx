import React from "react";
import { Typography } from "@mui/material";

export const Servicios = () => {
  return (
    <div>
      <Typography variant="h2"> Servicios Page</Typography>
    </div>
  );
};
