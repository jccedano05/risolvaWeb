import React from "react";
import { Typography } from "@mui/material";

export const Contacto = () => {
  return (
    <div>
      <Typography variant="h2"> Contacto Page</Typography>
    </div>
  );
};
